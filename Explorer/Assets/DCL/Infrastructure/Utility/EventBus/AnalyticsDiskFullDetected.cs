namespace Utility
{
    /// <summary>Lives in Utility rather than DCL.Analytics so UI-layer subscribers need no dependency on the analytics assembly.</summary>
    public readonly struct AnalyticsDiskFullDetected { }
}
